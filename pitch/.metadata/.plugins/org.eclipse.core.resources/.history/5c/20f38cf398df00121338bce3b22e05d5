import java.util.ArrayList;
import java.util.Collection;


public class GameFlow {
	private int team1GamePoints = 0;
	private int team2GamePoints = 0;
	private Collection<Card> playedCards = new ArrayList<Card>();
	private Collection<Player> players = ArrayList<Player>();
	
	
	public void playHand(int suit){
		for(Player player : playerCollection){
			if(player.canPlay(suit)){
				Card card = new Card();
				card = player.selectCard();
			}
		}
	}
	
	public void setPointers(int suitIdx){
		for(Player player : playerCollection){
			for(Card card : player.getHand()){
				if(card.getSuit() == suitIdx){
					if(card.getValue() == ){
				}
			}
		}
	}

}
